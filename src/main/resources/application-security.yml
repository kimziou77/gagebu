security:
  patterns:
    ignoring:
      ALL:
        - /
        - /static/**
        - /h2-console/**
        - /api/**/auth/**
        - /api/**/users
    permit-all:
      ALL:
        - /
        - /docs/**
      GET: [ ]
      POST: [ ]

cors:
  allowed:
    api: /**
    method: '*'
    origin:
      - http://localhost:3000
      - http://localhost:8080
      - http://kimyo.ga
      - http://34.125.126.248

jwt:
  issuer: gagebu
  secret-key: ${JWT_SECRET_KEY}
  access-token:
    expire-length: 1_800_000 # 30분

